name: samples
version: 0.1.0.0

dependencies:
  - base >= 4.7 && < 5
  - monad-par

ghc-options:
  - -threaded
  - -rtsopts
  - -with-rtsopts=-N

executables:
  main01:
    main: Main01.hs
    source-dirs: app
